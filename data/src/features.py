"""Stage 4 — Features.

Derive the engineered columns defined in the data contract (CLAUDE.md §4):
trip_duration_min, avg_speed_mph, fare_per_mile, tip_pct, is_cross_borough,
pickup_hour, pickup_day_of_week, time_of_day.

Input:  cleaned trips DataFrame.
Output: trips DataFrame with derived feature columns added.
"""

# import pandas as pd


def add_features(trips):
    """Compute the derived features required by the output schema."""
    # TODO: trip_duration_min, avg_speed_mph, fare_per_mile, tip_pct,
    # TODO: is_cross_borough, pickup_hour, pickup_day_of_week, time_of_day
    raise NotImplementedError
